# Version 15 用户可用库存脏数据修复版
一、版本概述
- 在 Version 14 基础上，修复了因 Redis 缓存与数据库事务不一致导致的库存脏数据问题。通过：
- 将缓存更新策略改为“删除缓存而非主动设置”
- 为所有业务缓存设置过期时间
- 集中管理 Redis Key 并封装缓存操作
- 提供统一的用户缓存清理方法（登出、事务回滚等场景调用）
- 确保库存数据在事务回滚后能正确恢复，避免脏数据残留。

Q1: version15之前的版本会存在什么问题？
A: 因为用户可用缓存存在bug，导致下单成功或下单失败时有redis脏数据，在下单失败时必现！